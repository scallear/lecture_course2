<%= render 'shared/title', title: @post.title %>

<h2><%= link_to @post.url, @post.url %></h2>
  <p>by <%= @post.creator.username %> at <span class='quiet'><%= @post.created_at %></span></p>
  <p><%= @post.description %> </p>
