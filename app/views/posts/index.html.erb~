<%= render 'shared/title', title: 'All Posts' %>

<div class='row'>
  <% @posts.each do |post| %>
    <div class='span8 well'>
      <h2><%= link_to post.title, post.url %> (<%= link_to 'Edit', edit_post_path(post) %>)</h2>
      <p>by <%= post.user_id ? post.creator.username : 'Unknown' %> at <span class='quiet'><%= post.created_at %></span></p>
      <p><%= post.description %> </p>
      <%= link_to 'Comments', post_path(post[:id]) %>
    </div>
  <% end %>
</div>
